version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
      timezone: "Europe/London"
    open-pull-requests-limit: 10
    labels:
      - "dependencies"
    # Hold TypeScript majors: typescript-eslint's peer range is
    # typescript >=4.8.4 <6.1.0, so 7.x cannot install (#507 and #569 sat
    # red). TS 7 support is tracked in typescript-eslint/typescript-eslint#10940.
    ignore:
      - dependency-name: "typescript"
        update-types:
          - "version-update:semver-major"
    groups:
      react:
        patterns:
          - "react"
          - "react-dom"
          - "react-grid-layout"
          - "@types/react"
          - "@types/react-dom"
      nextjs:
        patterns:
          - "next"
          - "eslint-config-next"
          - "@next/*"
      sentry:
        patterns:
          - "@sentry/*"
      serwist:
        patterns:
          - "serwist"
          - "@serwist/*"
      playwright:
        patterns:
          - "@playwright/test"
          - "@axe-core/playwright"
      vitest:
        patterns:
          - "vitest"
          - "@vitest/*"
          - "jsdom"
      testing-library:
        patterns:
          - "@testing-library/*"
      linting:
        patterns:
          - "eslint"
          - "husky"
          - "lint-staged"
      build-tools:
        patterns:
          - "typescript"
          - "postcss"
          - "autoprefixer"
          - "tailwindcss"
      type-definitions:
        patterns:
          - "@types/node"

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
      timezone: "Europe/London"
    open-pull-requests-limit: 10
    labels:
      - "dependencies"
    groups:
      github-actions:
        patterns:
          - "*"
